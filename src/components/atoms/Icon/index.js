import Icon, { ICON_CATEGORY_NAMES, ICON_NAMES, IconOptions } from './Icon';
import fixtures from './fixtures';

export default Icon;
export { Icon, fixtures, ICON_CATEGORY_NAMES, ICON_NAMES, IconOptions };
